<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Add Review</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"/>
  <style>
    body {
      background-color: #121212;
      color: #e0e0e0;
      font-family: 'Inter', sans-serif;
      padding: 2rem;
    }

    .review-container {
      max-width: 800px;
      margin: auto;
    }

    .book-cover {
      max-height: 300px;
      object-fit: cover;
      border-radius: 12px;
    }

    a.back-link {
      color: gold;
      text-decoration: none;
      display: inline-block;
      margin-bottom: 1rem;
    }

    label {
      margin-top: 1rem;
    }

    .form-control,
    .form-select {
      background-color: #1e1e1e;
      color: #e0e0e0;
      border: 1px solid #333;
    }

    .form-control:focus {
      border-color: #ffc107;
      background-color: #1e1e1e;
      color: #fff;
    }

    .btn-gold {
      background-color: #ffc107;
      color: #121212;
      border: none;
    }

    .btn-gold:hover {
      background-color: #e0a800;
    }
   textarea ::placeholder {
  color: white;
  opacity: 1; /* Firefox */
}

textarea:-ms-input-placeholder { /* Edge 12 -18 */
  color: white;
}
  </style>
</head>
<body>
  <div class="review-container">
    <a href="/" class="back-link">&larr; Back to Reviews</a>
    <hr/>

    <!-- Form to add review -->
    <form action="/submit-review" method="POST">
      <input type="hidden" name="bookId" value="123" />
<div class="mb-3">
        <label for="title" class="form-label">Book Title</label>
        <input type="text" class="form-control " id="title" name="title" required />
      </div>
  

      <!-- <div class="mb-3">
        <label for="readdate" class="form-label">Date Read</label>
        <input type="date" class="form-control" id="readdate" name="readdate" required />
      </div> -->

      <div class="mb-3">
        <label for="rating" class="form-label">Rating (out of 5)</label>
        <select class="form-select" id="rating" name="rating" required>
          <option selected disabled value="">Choose...</option>
          <% for(let i = 5; i >= 0.5; i -= 0.5) { %>
            <option value="<%= i %>"><%= i %></option>
          <% } %>
        </select>
      </div>

      <div class="mb-3">
        <label for="review" class="form-label">Your Review</label>
        <textarea class="form-control" id="review" name="review" rows="12" placeholder="Enter your Review" required></textarea>
      </div>

      <button type="submit" class="btn btn-gold mt-2">Submit Review</button>
    </form>
  </div>
</body>
</html>
